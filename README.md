# Copilot CI Python Lab

Small Python project for the C270 CI lab.

## Run locally

```powershell
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
pytest
flake8 src tests
```
